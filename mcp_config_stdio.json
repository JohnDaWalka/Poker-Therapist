{
  "mcpServers": {
    "dossier": {
      "type": "stdio",
      "command": "python",
      "args": ["-m", "dossier.mcp_server"],
      "env": {
        "DATABASE_URL": "${workspaceFolder}/.env"
      }
    }
  }
}
